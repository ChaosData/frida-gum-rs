[package]
name = "testhook"
version = "0.1.0"
authors = ["jtd"]

[lib]
name = "testhook"
crate-type = ["dylib"]

[dependencies]
gumshoe = { path = "../.." }
libc = "0.2"

[build-dependencies]
gcc = "0.3"
gumshoe = { path = "../.." }

