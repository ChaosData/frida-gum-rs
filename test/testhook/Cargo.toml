[package]
name = "testhook"
version = "0.1.0"
authors = ["jtd"]

[lib]
name = "testhook"
crate-type = ["cdylib"]

[dependencies]
gumshoe = { path = "../.." }
libc = "0.2"

[build-dependencies]
gumshoe = { path = "../.." }
cc = "1.0"
base64 = "0.13"
